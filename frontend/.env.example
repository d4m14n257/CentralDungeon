# Copy this file to .env.local and adjust if needed. .env.local is gitignored - Vite
# loads it automatically (see docs/arquitectura.md §6.1). Default already matches the
# backend's local dev port, so most local setups don't need this file at all.
VITE_API_BASE_URL=http://localhost:8080
