spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/centraldungeon?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:centraldungeon}
    password: ${DB_PASSWORD:centraldungeon}
  jpa:
    show-sql: true

app:
  cookie:
    secure: false # localhost is http, not https

logging:
  level:
    com.centraldungeon: DEBUG
